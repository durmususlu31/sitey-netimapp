rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Kurallar: Giriş yapan tüm yetkili yöneticiler ve kullanıcılar için okuma/yazma
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

